name: woongjin_airflow_env
channels:
  - defaults
dependencies:
  - python=3.11
  - pandas
  - numpy
  - apache-airflow
  - requests
  - pip
  - pip:
      - google-cloud-bigquery
      - slack-sdk
      - boto3

